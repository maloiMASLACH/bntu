export interface CreateNewPlaceProps {
  handleChange: () => void;
}

export interface CreateNewPlaceFormProps {
  handleClose: () => void;
  handleChange: () => void;
}

export interface CreateFormType {
  building: string;
  auditory: string;
}
